apiVersion: v1
kind: Service
metadata:
  name: chatbot-ui-service
  namespace: chatbot-app
spec:
  selector:
    app: chatbot-ui
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
