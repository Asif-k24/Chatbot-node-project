version: '3.9'

services:
  chatbot-ui:
    image: asif0098/chatbot-ui:latest
    container_name: chatbot-ui
    build:
      context: ../
      dockerfile: docker/Dockerfile
    ports:
      - "3000:3000"
    restart: always

